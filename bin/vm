#!/usr/bin/env python

from os import chdir, execvp
from pathlib import Path
import sys

ansible_dir = (Path(__file__) / '..' / '..' / 'ansible').resolve().as_posix()

chdir(ansible_dir)

execvp('vagrant', ['vagrant'] + sys.argv[1:])
