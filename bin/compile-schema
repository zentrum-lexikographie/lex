#!/usr/bin/env python

from pathlib import Path

import lein


build_dir = (Path(__file__) / '..' / '..' / 'build').resolve().as_posix()

lein.run(['run', '-m', 'zdl-lex-build.schema'], cwd=build_dir)
